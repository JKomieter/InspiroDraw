

const boardHandler = (socket) => {
    const DrawHandler = ({}) => {
        
    }

    const createBoardSharingHandler = ({}) => {
        // get the board details from the DB
    }

    socket.on('draw', DrawHandler);
};